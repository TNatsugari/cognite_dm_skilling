[cdf]
default_organization_dir = "rmdm"
default_env = "dev"

[modules]
# This is the version of the modules. It should not be changed manually.
# It will be updated by the 'cdf modules upgrade' command.
version = "0.6.19"

[alpha_flags]
upload = true

[plugins]
run = true
pull = false
dump = true
